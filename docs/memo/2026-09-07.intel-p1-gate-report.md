# intel 理解层 P1-Gate 最终评估报告

生成时间：2026-09-07 17:08
评估范围：v2 prompt 全量重跑结果（intel schema，PostgreSQL）
标注样本：`data-cleaner/p1_gate_sample.csv`（107 条人工标注，已落盘）

## 1. P1-Gate 四项硬指标

| 指标 | 阈值 | 实测 | 数据源 | 结论 |
|---|---|---|---|---|
| symbol 合规率 | ≥90% | **100%**（378/378） | 机器（全量 v2 mention） | ✅ PASS |
| span 有效率 | ≥95% | **100%** | 机器（全量 v2 mention） | ✅ PASS |
| 幻觉率 | ≤2% | **0%**（机器 0% + 人工标注 0/107） | 机器 + 人工 | ✅ PASS |
| stance 一致率 | ≥80% | **100%**（107/107） | 人工标注 | ✅ PASS |

**结论：P1-Gate 四项全 PASS，理解层通过质量门，进入 P2（画像）。**

## 2. 人工标注明细（老朱标注，107 条）

- 标注列：`人工_标的` / `人工_倾向` / `是否幻觉` 三列 107/107 全填。
- stance 分布（人工 vs LLM 完全一致）：

  | stance | LLM 数 | 人工数 | 一致 |
  |---|---|---|---|
  | bullish | 8 | 8 | ✅ |
  | neutral | 94 | 94 | ✅ |
  | bearish | 5 | 5 | ✅ |

- 不一致条数：**0**
- 幻觉标记=是：**0**（107 条全 "否"）

## 3. v2 vs v1 前后对比（修复成效）

| 指标 | v1 | v2（全量） |
|---|---|---|
| llm_runs ok | 12 | 471 |
| doc_mentions | 6 | 378 |
| 失败率 | 68% | 0%（api_fail 0） |
| quarantine | 16+ | 16（隔离率 2.7%） |
| 单篇成本 | 超时/429 拥塞 | ¥2.10 / 全量 |
| 召回倍数 | 1× | ≈60× |

v1 三重复合根因已全部修复：
1. `enable_thinking` 位置错误（payload 顶层 → `chat_template_kwargs`）→ 思考关闭、零超时零 429。
2. `mention` 单数 + 规则过严 → v2 `mentions` 数组 + 放宽触发，一篇多标的不再漏抽。
3. span 严格偏移误杀 → `validate_span` 加原文搜索还原层，正确抽取不再被 quarantine。

## 4. 已知偏差 / Caveat（P2 需持续监控）

- **非中性样本偏少**：107 条标注中 neutral 占 94 条（88%），bullish/bearish 合计仅 13 条。
  stance 一致率 100% 虽漂亮，但"看多/看空判断准确"这一能力点在样本上**验证不足**。
  P2 进入画像后，应持续监控 bullish/bearish 类 mention 的准确率，必要时扩充带观点的语料重测。
- **财经 RSS 偏事实陈述**：数据特征决定了中性占比天然高，符合预期，但意味着模型"观点抽取"能力尚未被充分压测。

## 5. 下一步

- ✅ P1-Gate 通过 → 进入 **P2 画像**（基于已抽取的 378 条 mention 构建标的观点画像）。
- P2 建议：先用当前 v2 落库的 mention 跑画像基线；对 bullish/bearish 样本做定向复核。
