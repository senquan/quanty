# 2026-07-23 技术经验提取

## 主干变更摘要
自上次同步（`13d7208`）以来，master 新增 **1 个提交**：

| Commit | 说明 |
|--------|------|
| `360c119` feat: 因子库相关页面 | 新增因子库前端页面、后端菜单API、更新logo |

## 技术经验分类

### 🚀 新增功能
- **因子库相关页面**：完成因子库模块的前后端闭环，包含因子列表展示、因子详情编辑、回演工作室入口等
  - 涉及模块：
    - **前端**：`frontend/apps/web-ele/src/views/data/factor/` 下多个 Vue 组件（factor-library.vue, factor-card.vue, factor-editor-modal.vue, backtest-studio.vue, correlation-heatmap.vue, efficacy-deep-dive.vue）
    - **后端**：`backend/app/api/api_v1/endpoints/menu.py` — 新增因子库菜单端点，`api.py` 路由注册更新
  - 技术要点：
    - 使用 Element Plus + Vue 3 Composition API 构建数据表格和卡片组件
    - 前端组件化设计：factor-card 卡片封装、factor-editor-modal 弹窗编辑器分离关注点
    - 后端菜单驱动的动态路由配置，支持因子库多页面自动注册
  - 设计模式：
    - **策略模式**回演工作室 — 不同因子类型适配不同回演策略
    - **组合模式**组件树 — 父容器管理子组件状态（如 factor-library 管理卡片列表）

### 🛠️ 基础设施优化
- **前端配置统一**：`main.ts` + `preferences.ts` 修改，可能涉及插件注册和全局偏好设置重构
  - 效果：提升应用启动时的配置灵活性，减少硬编码
- **Logo 更新**：`logo.svg` 小幅修改，品牌视觉对齐

### 📝 文档体系
- **Roadmap 落地**：`quant-closed-loop-roadback.md`（215行）系统性规划因子闭环流程
- **日常计划文件沉淀**：docs/plans/ 下大量历史 report.md / insights.md 积累，形成完整的项目演进记录链

### 📦 依赖/配置变更
- 从 diff stat 看无直接 deps 变更，但 UI 层多处组件改动暗示前端工具链可能升级（Vite/Webpack 版本迭代常见于此类重构）

## 💡 经验总结

1. **菜单驱动的动态路由策略值得复用**：后端通过 API 端点注册前端页面路径，主路由文件自动加载 — 新页面只需新增 endpoint + Vue 组件即可上线，减少路由维护成本。
2. **因子模块的组件化抽象较成熟**：从 factor-card → factor-editor-modal → backtest-studio 的层级拆分，体现了从高内聚到可交互的设计思路。后续因子库扩展时可直接参考此模式。
3. **文档即代码的实践**：roadmap.md + daily report/insights 组合形成完整的决策追溯链，commit message → plan → code → insight 链路清晰。建议持续使用。
4. **前端配置集中化趋势**preferences.ts 的增改反映应用从硬编码向可配置方向演进，为后续多环境部署（dev/prod/staging）打下基础。

## 📝 待跟进事项

- [ ] factor-library.vue 等组件的 props/events 类型定义是否完整 — TypeScript 覆盖度待验证
- [ ] 后端菜单 API (`api_v1/endpoints/menu.py`) 是否需要加入权限校验中间件
- [ ] `preferences.ts` 新增配置项是否同步到多环境配置（.env / config.yaml）
- [ ] 分支分歧较大，待网络恢复后执行 git pull --rebase 保持线性历史
