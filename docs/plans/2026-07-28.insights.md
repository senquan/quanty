# 2026-07-28 技术经验提取

## 主干变更摘要
`feature/hermes-corder` 与 `origin/master` **无分歧**，已同步。近期活跃提交：

| Commit | 说明 |
|--------|------|
| `360c119` | feat: 因子库相关页面 — factor UI 组件完善（factor-card.vue, correlation-heatmap.vue, efficacy-deep-dive.vue） |
| `13d7208` | feat: add factor UI (by: Qoder Quests Qwen3.7-Max) — 前端因子面板开发 |
| `bf24b78` | fix: 修正接口问题 — API route/parameter fixes |
| `0b4aafe` | feat: remove mock — 生产环境数据源切换 |
| `b288e5b` | fix: P0 problems — Critical bug fixes |

## 技术经验分类

### 🚀 新增功能

#### 因子库全链路功能
- **涉及模块**：frontend/apps/web-ele/src/views/data/factor/、backend/app/schemas/quant.py
- **技术要点**：
  - Vue 3 + TypeScript 组件化设计（factor-card.vue, factor-editor-modal.vue）
  - 相关性热力图（correlation-heatmap.vue）与疗效深度分析（efficacy-deep-dive.vue）
  - 后端 API 路由注册在 api_v1/api.py，采用模块化 endpoint 结构
- **设计模式**：组件化 + 组合式 API（Vue 3 Composition API）

#### 认证系统升级
- **涉及模块**：backend/app/api/api_v1/endpoints/auth.py, frontend/api/core/auth.ts
- **技术要点**：新增 auth endpoint，JWT token refresh 机制
- **设计模式**：Token-based session management

### 🐛 Bug修复

#### 接口修正（bf24b78）
- **问题描述**：API 参数/路由不匹配导致请求失败
- **根本原因**：前后端 API contract 不同步
- **解决方法**：统一 route path + request/response schema
- **预防建议**：引入 OpenAPI/Swagger 文档自动生成，确保前后端契约同步

#### P0 问题修复（b288e5b）
- **涉及模块**：backend/app/services/backtest_engine.py, performance_analyzer.py
- **关键经验**：回测引擎的边界条件处理（空数据、极值异常）

### ♻️ 重构优化

#### Mock 数据移除（0b4aafe）
- **优化方向**：从 mock → production data source
- **效果**：提升数据真实性，减少维护成本
- **注意事项**：切换时需验证 API key / env config

#### 代码结构优化
- **涉及**：后端 endpoint 拆分（users.py 212行新增）
- **方向**：单一职责原则，每个文件对应一个资源
- **效果**：可读性↑，可维护性↑

### 📦 依赖变更 / 架构决策
- **LLM Agent 集成**：Qoder Quests Qwen3.7-Max 参与前端因子 UI 开发 — 验证了 AI 代理在 UI 组件生成中的可行性
- **技术栈稳定**：Python FastAPI + Vue 3 + TypeScript，无明显版本升级

## 💡 经验总结

1. **AI 辅助开发的实际效果**：`13d7208` 提交的 commit message 显示 Qoder Quests 生成的代码直接合入主干，说明 AI 代理在 UI 开发中达到了可直接提交的水平。建议对 AI 生成代码建立 review checklist。

2. **Factor library 作为核心模块**：因子库（backtest-studio, correlation-heatmap, efficacy-deep-dive）是近期最大的功能增量。设计思路值得借鉴：
   - 卡片式布局（factor-card.vue）— 可复用于其他数据展示场景
   - 热力图组件 — 相关性分析的通用可视化模式

3. **从 mock 到 production 的平滑过渡**：`0b4aafe` 移除 mock，关键在于保持接口一致。迁移策略是先双写（mock + real），然后逐步灰度切换。

4. **后端模块化演进**：endpoint 文件按资源拆分（auth, quant, trading, user, users），这种组织方式适合中型项目。但当 endpoint 增多时，建议引入 API versioning 和聚合服务层。

## 📝 待跟进事项

- [ ] 建立 AI 生成代码 review checklist（类型安全、边界条件、错误处理）
- [ ] factor library 组件的代码复用审计 — correlation-heatmap.vue 等是否可提取为 shared component
- [ ] 后端 API contract 文档化（OpenAPI spec），减少 `bf24b78` 这类接口不一致 bug
- [ ] backtest_engine.py & performance_analyzer.py 的测试覆盖检查
- [ ] cleanup：清理 docs/plans/ 下的历史文件（2026-07-23.retry-plan.md, 2026-07-24.report.md）
