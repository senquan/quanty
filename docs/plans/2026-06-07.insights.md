# 2026-06-07 技术经验提取

## 📌 分析范围

feature/hermes-corder 分支与 origin/master 完全合并。基于上次分析（`0b4aafe` - `feat: remove mock`）的变更继续回顾。

## ✅ 关键经验（回顾前日 commit）

### 1. FastAPI + SQLAlchemy async/session管理规范
- 所有endpoint统一使用 `AsyncSession = Depends(get_db)` 
- count query与data query的WHERE条件须保持一致（分页总数不匹配的前端陷阱已修复）
- serialize helper函数模式可复用到所有Entity

### 2. Endpoint Router模块化注册
- 各业务域独立维护endpoints，主API只负责register
- 新增endpoint无需修改注册文件，降低merge conflict概率

### 3. SQLAlchemy 2.0 `delete()`语法
- 新代码使用`delete(Model).where(...)`而非旧版`func.delete()` 
- MySQL方言兼容性需验证

## ⚠️ June 6→7无新代码提交
branch状态稳定，等待新功能开发或下一次master pull。
