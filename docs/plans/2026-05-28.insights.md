# 2026-05-28 技术经验提取

## 主干变更摘要

`origin/master` 相比 `feature/hermes-corder` **无新增提交** — 两者 merge-base 已在 `21652f9 chore: add claude.md`。

> 上次增量变更（与 2026-05-27 对比）：
> - 主干 merge PR #6 后添加了 `CLAUDE.md` 和 karpathy 规则文件
> - 该变更已完整同步至 feature/hermes-corder

## 技术经验分类

### 🚀 新增功能 / 文档改进
- **CLAUDE.md 和 .qoder/rules/karpathy-guidelines.md**
  - 涉及模块：项目根目录 / .qoder/rules
  - 技术要点：引入 Claude Code 和 Qoder IDE 的规则文件，定义项目编码规范和开发指南
  - 设计模式：规则即文档（Rules-as-Documentation）— 通过配置文件引导 AI 和开发者遵循统一规范
  - 经验：将项目约定固化到 `.qoder/rules/` 和 `CLAUDE.md` 中，可使新成员和 AI agent 更快理解项目风格

### 🐛 Bug修复（近期）
- **`update_menu_status` 返回值丢失**（已在 2026-05-27 insights 中记录）
- **Query 默认参数简化**（已在 2026-05-27 insights 中记录）

## 💡 经验总结
1. **分支同步策略**: feature 分支应定期 rebase master 以保持一致，避免长期 diverge
2. **CLI 工具规则文件**: `.qoder/rules/karpathy-guidelines.md` 和 `CLAUDE.md` 作为 AI agent 和人类开发者的共同参考，建议保持更新
3. **同步 SQLAlchemy 改造**: `AsyncSession → Session` 的迁移已在本次分支中完成，注意 `await db.execute()` 仍需 Awaitable（FastAPI 自动处理）

## 📝 待跟进事项
- 检查 `frontend/apps/web-ele/package.json` 是否有依赖更新需处理
- 确认 `.step_cache.json` 和 `run_daily.sh` 的清理不影响现有工作流
