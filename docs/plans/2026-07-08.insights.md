# 2026-07-08 Technical Insights

## Master Commit 360c119: "feat: 因子库相关页面" (Jun 22)

**Scope:** 14 files (+266 insertions, -29 deletions) across backend, frontend, config, and docs.

### Key Observations

#### 1. FastAPI Endpoint Architecture — Modular Router Pattern
The project follows a clear module-level endpoint organization:
```
api.py (assembly layer)
├── auth.router      → /auth
├── user.router      → /user (singleton vs collection pattern)  
├── users.router     → /users
├── roles.router     → /roles
│
endpoints/ directory
└── menu.py (new 12-line stub)
    └── @router.get("/all") → async API endpoint
```

**Key Convention:** Endpoints return `Response[List[MenuInDB]]` style (unified response wrapper). The endpoint files are thin controllers with minimal logic — business logic deferred to service layer.

#### 2. Vue3 + ElementPlus UI Consistency Refactor
6 factor-related components simultaneously standardized:
- **Font-size**: Systematic `text-[10px] → text-[11px]` replacement for better readability in dense dashboard metrics
- **Card shadows**: `<ElCard shadow="hover">` for interactive data cards, `"never"` for static decorations — reduces visual noise
- **Metrics display**: Color-coded values using Tailwind classes (emerald=positive, rose=negative, gray=neutral label)
- **Grid layout**: 2x2 responsive grid with `lg:grid-cols-4` breakpoint at large screens

This pattern could be extracted into a reusable `<MetricCard>` Vue component for project-wide consistency.

#### 3. Application Config — Brand Asset Integration
```typescript
// preferences.ts additions:
logo: {
  enable: true,
  fit: 'contain',
  source: '/static/logo.svg',        // light mode asset
  sourceDark: '/static/logo-dark.svg' // dark mode (TODO: create)
}
accessMode: 'frontend'  // simplified from 'mixed' — auth handled purely on client side
```

**Note:** Dark mode logo referenced but not built yet. Access mode change implies the backend API no longer validates session tokens for this app context.

### 📋 Technical Debt from Master Commit
1. `menu.py` is a stub (`return Response.success(data=[])`) — needs actual database query implementation (SQLAlchemy?)
2. Dark logo SVG missing — `sourceDark` reference will result in 404 until created
3. All factor UI components use hardcoded mock data — API integration layer needed

### 💡 Extracted Reusable Patterns
- **Task table convention**: "任务 | 说明 | 验收标准" used consistently across roadmap milestones
- **IC/IR validation metrics**: IC > 0.02, IR > 0.8 thresholds defined for factor quality gates
- **Color coding standardization**: Positive → emerald (green), Negative → rose/red — consistent with Chinese market convention (red=rise, green=fall) and international (green=good, red=bounce) hybrid

## Pending Action Items
1. Implement `menu.py` database query layer (currently empty stub)
2. Create dark mode logo SVG for production consistency  
3. Extract common `<MetricCard>` component from factor UI patterns
4. Verify `accessMode: 'frontend'` doesn't break backend auth middleware flow
