# 2026-09-21 技术经验提取

## 主干变更摘要

**今日 master 无新增提交。** merge-base / master HEAD 均为 `7512c39 feat: 新增手动权重模式并统一股息率百分比口径`（2026-09-17）。

该提交的完整分析见 `2026-09-20.insights.md`，要点回顾：
- **手动权重模式**：新增因子权重手动模式，与自动模式并存
- **股息率口径统一**：同步统一股息率百分比口径，防止双口径并存

## 技术经验分类

### 🚀 新增功能
- （无新提交，无新增功能）

### 🐛 Bug修复
- （无新提交，无新增 Bug 修复）

### ♻️ 重构优化
- **拓扑回落稳态被打破（流程经验）**：09-20 记录的"rebase 落 master → push 被拒 → pull --rebase 回落旧基点"稳态今日未复现——本次 rebase 后 push 直接成功，分支历史真正落在 master HEAD `7512c39` 之上（`merge-base --is-ancestor` 验证通过）。
  - 经验：pull --rebase 回落是"非 fast-forward push 被拒"的次生结果，而非必然。当远端 feature 未领先本地时，rebase 后的 push 可直接 fast-forward 成功。
  - 效果：历史拓扑与 master 对齐，后续 `git log feature..origin/master` 可直接反映真实主干差异。

### 📦 依赖变更
- （无新提交，无依赖变更）

## 💡 经验总结

1. **每日验证内容对等而非拓扑**：即使分支拓扑未落在 master 之上，`git diff --stat origin/master feature -- . ':!docs/plans'` 为空即说明内容完整包含 master 变更，分析结论不受历史拓扑影响。
2. **无新提交日也要留痕**：记录 master HEAD hash + 消息，引用上次 insights 的要点，避免 insights 链断裂。
3. **rebase 跳过 previously-applied 属正常**：feature 历史上 cherry-pick 过 master 提交，rebase 按 patch-id 自动去重，skip 数量只作参考。

## 📝 待跟进事项

- （无新增待办）
- 沿用 09-20 遗留：`f1a8491` 微信公众号文章自动投喂实施方案仍为"待评审"状态，建议推动评审。
