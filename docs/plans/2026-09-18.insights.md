# 2026-09-18 技术经验提取

## 主干变更摘要
**今日无新增主干提交。** `git fetch origin master` + `git rebase origin/master` 后，`feature/hermes-corder..origin/master` 为空，本地分支已完全包含 master 全部历史（41 个领先提交均为本分支的每日报告提交）。

本次同步过程中观察到的有价值信息（来自已在本分支中的近期 master 提交）：

- `7512c39` feat: 新增手动权重模式并统一股息率百分比口径
- `3cd19de` fix(intel): 013 不再重建已废弃的 3 列唯一索引，修 dc 启动即崩（D-9 回归）
- `f1a8491` docs(intel): 微信公众号文章自动投喂 实施方案（待评审）
- `c7a4686` docs: 存量文档统一日期前缀，并确立 plans/memo 分工

## 技术经验分类

### 🚀 新增功能
- **手动权重模式 + 股息率口径统一**（7512c39，已在本分支）
  - 涉及模块：权重/因子配置
  - 技术要点：新增"手动权重模式"，同时统一了股息率的百分比口径
  - 经验：引入新计算模式时同步统一相关字段口径，避免新旧模式混用导致数值不可比

### 🐛 Bug修复
- **dc 启动即崩回归**（3cd19de，已在本分支）
  - 问题描述：数据库迁移 013 尝试重建已废弃的 3 列唯一索引，导致 dc 启动即崩溃
  - 根本原因：迁移脚本与现状 schema 不一致（D-9 回归）
  - 解决方法：013 不再重建已废弃索引
  - 预防建议：迁移脚本对"可能不存在/已废弃"的索引应做存在性检查（`IF NOT EXISTS` / 动态 SQL），并在合入前跑一遍完整启动流程验证

### ♻️ 重构优化
- **文档体系统一**（c7a4686，已在本分支）
  - 优化内容：存量文档统一日期前缀，确立 plans/memo 分工
  - 效果：文档可按日期检索，计划类与备忘录类不再混杂

### 📦 依赖变更
- 无

## 💡 经验总结
1. **rebase 大量跳过提交是正常现象**：本分支此前已 cherry-pick/包含 master 多数提交，rebase 时跳过 29 个"previously applied"提交、重放 4 个，属幂等收敛，无需干预
2. **迁移脚本必须容忍 schema 现状**：`CREATE UNIQUE INDEX` 前应检查索引是否已存在或已被废弃，防止启动即崩
3. **口径统一要随模式变更一起做**：新增手动权重模式时同步统一股息率百分比口径，防止双口径并存
4. **网络环境**：本次 fetch 第 1 次尝试即成功，WSL2 mirror 模式下 Git HTTPS 走 credential store 已稳定

## 📝 待跟进事项
- `f1a8491` 微信公众号文章自动投喂方案仍处于"待评审"状态（`docs/plans/2026-09-11.wechat-article-auto-ingest.md` 相关），需推动评审结论
- 手动权重模式合入后，建议验证历史仓位/净值在新口径下的可比性
