# 2026-07-09 技术经验提取

## 主干变更摘要

今日无新增主干 commit。master 与 feature/hermes-corder 之间的差异仅为旧报告文件的删除（历史 report.md / insights.md 清理）。

feature/hermes-corder 分支已同步到 origin/master 最新状态。

## 技术经验分类

### 🚀 近期项目模式观察
- **项目类型**: Quanty - 量化交易系统，Python + FastAPI + SQLAlchemy Async + PostgreSQL + Vue 3/TypeScript
- **任务自动化**: 建立了完善的定时任务框架，每日自动同步主干、检查任务、生成报告和提取经验
- **分支策略**: feature/hermes-corder 功能分支通过每日 rebase 与 master 保持同步

### 🐛 常见 Bug 类型（基于历史提交）
- **AsyncSession 混用**: quant.py 中 sync Session 与 async Session 混用导致的类型错误
  - 预防建议: 统一使用 AsyncSession，避免同步/异步混用
- **前后端类型不匹配**: Vue 3 前端 icon / 后端数据字段类型不一致

### ♻️ 重构优化方向（基于历史提交）
- **AsyncSession 全面替换 sync Session**: quant.py 中所有 db.execute/commit 改为异步模式
  - 已实现: commit b288e5b (2026-06-03)
  - 效果: 提升并发安全性，消除类型错误
- **PUT/DELETE API 补全**: FastAPI 路由中 RESTful 完整 CRUD 覆盖

### 📦 技术栈与依赖（需关注）
| 依赖 | 版本状态 | 说明 |
|------|----------|------|
| FastAPI | 待升级 | 建议跟进最新稳定版，注意 ASGI 兼容性 |
| SQLAlchemy | 待升级 | AsyncSession 迁移需确认 API 变化 |
| Pydantic | 待升级 | v2 版本有 breaking changes |

## 💡 经验总结

1. **定期同步策略有效**: feature 分支通过每日 rebase 与 master 保持同步，减少了大合并冲突的风险
2. **网络重试机制工作正常**: fetch 和 push 均在首次尝试即成功，3 次重试+30秒间隔的机制可靠
3. **经验分类模板化**: 使用标准化的分类模板（新功能/Bug修复/重构/依赖），便于后续追溯
4. **文档即代码**: report.md + insights.md 双文档结构形成完整的项目演化历史
5. **增量分析原则**: 只分析新增 commit 的变更，避免重复工作；无提交日也需生成报告保证链路完整

## 📝 待跟进事项

- [ ] 确认项目依赖（FastAPI / SQLAlchemy / Pydantic）是否需要升级到最新稳定版
- [ ] 根据历史 P0-P3 优先级分类，评估是否有新的高优先级问题出现
- [ ] 检查 quant.py AsyncSession 迁移的完整性（是否所有操作点都已异步化）
