# 2026-06-05 技术经验提取

## 主干变更摘要
本次同步未发现 origin/master 上的新提交。最后已知 master HEAD：

| Commit | 消息 | 日期 |
|--------|------|------|
| `0b4aafe` | feat: remove mock | 2026-06-02 |

feature/hermes-corder 分支在 merge-base (`0b4aafe`) 之上已有 4 个独有 commit（每日任务报告），rebase 时跳过 4 个已合并的 cherry-pick。

## 技术经验分类

### 🚀 最近新增功能
基于主干最新已知提交 `0b4aafe feat: remove mock`：
- **功能名称**：移除 Mock 实现
  - 涉及模块：待后续详细分析
  - 技术要点：用真实实现替代测试 mock
  - 设计模式：策略模式 / 依赖注入

### 📋 分支整合经验
- feature/hermes-corder 使用 cherry-pick 方式集成 master 变更
- rebase 时遇到 `skipped previously applied commit` 提示属正常行为（cherry-pick 提交在 rebase 阶段被识别为已存在）
- **最佳实践**：定期从 master rebase 而非 merge，保持线性历史

## 💡 经验总结
1. **Cherry-pick + Rebase = 重复提交**：当前 workflows 中 feature branch 通过 cherry-pick 集成 master，再 rebase 时 git 自动跳过已存在的 commit — 可考虑直接用 `git merge origin/master` 替代 cherry-pick 流程
2. **每日报告任务无任务文件时静默处理**：当 tasks.md 不存在时，SKIP_TASKS=true 是合理策略
3. **网络重试机制有效**：fetch 在第 1 次成功，无需回退到第 2、3 次重试

## 📝 待跟进事项
- [ ] 分析 `0b4aafe feat: remove mock` 涉及的代码变更（需要查看具体 diff）
- [ ] 评估是否将 cherry-pick 流程替换为 merge，简化集成工作流
- [ ] 确认今日无任务文件的决策是否与用户 task 规划一致
