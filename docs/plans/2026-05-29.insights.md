# 2026-05-29 技术经验提取

## 主干变更摘要

**无新增 upstream commit** — `origin/master` 相对于 `feature/hermes-corder` 无新提交。

`feature/hermes-corder` 领先 `origin/master` 18 个 commits（均为每日报告 bot commits），上次主干差异来自之前的同步周期，已在历史 insights（2026-05-20 至 2026-05-27）中完整记录。

> **历史上一次增量变更**: `AsyncSession → Session` 迁移、`update_menu_status` 返回修复、CORS 配置名修正、`StrategyUpdate` schema 移除、旧文件清理。详见 `2026-05-20.insights.md`。

## 💡 经验总结

### 1. 分支持续领先是正常模式
`feature/hermes-corder` 已独立开发了 18 天（从 2026-04-26 到 2026-05-28），而 `origin/master` 停留在 `21652f9`。这说明：
- 项目使用 feature-branch 工作流，主干变更频率低
- feature 分支积累了大量独立的 bot commits（report/insights/task 文件自动更新）
- **建议**: 定期将 feature 分支的最新状态 merge/rebase 到 master，避免长期 diverge

### 2. Bot commit 模式识别
所有 18 个领先 commits 都是 `Daily task report & insights:` 格式，说明自动化任务系统（cron + daily task runner）稳定运行。关键点：
- 每日自动同步主干（fetch + rebase）
- 自动检查任务文件并执行
- 自动生成报告和 insights
- 即使无任务，也会生成 report.md 和 insights.md 维持审计轨迹

### 3. 分支同步策略建议
鉴于 feature 分支领先 18 commits 且主干无新变化：

| 场景 | 建议操作 |
|------|--|
| feature 分支稳定且有新功能 | merge `feature/hermes-corder` → `origin/master` |
| feature 分支有 bug | cherry-pick 修复到 master |
| master 有新提交后 | `git rebase origin/master` 到 feature 分支 |

### 4. 代码变更模式回顾（历史）
基于历史 insights，项目近期的主要变更模式：

| 变更类型 | 描述 | 相关文件 |
|--|---|--|
| **Async → Sync** | SQLAlchemy 从异步迁移到同步 Session | `quant.py`, `dependencies.py` |
| **端点精简** | 移除 unused endpoint (`update_strategy`, `delete_strategy`) | `quant.py` |
| **schema 清理** | 移除不再需要的 schema (`StrategyUpdate`) | `schemas/quant.py` |
| **Bug 修复** | `update_menu_status` 返回语句位置修复 | `menus.py` |
| **配置标准化** | `allowed_origins_list` → `ALLOWED_ORIGINS` | `main.py` |
| **文档清理** | 删除旧 report/insights 文件 (~1200+ 行) | `docs/plans/` |

### 5. 待跟进事项

1. **[高]** 评估是否将 `feature/hermes-corder` 的最新代码 merge 到 `origin/master`（已领先 18 commits）
2. **[中]** 确认 `origin/master` 停留在 `21652f9` 是否有意为之（开发暂停？合并完成？）
3. **[低]** 检查 `feature/hermes-corder` 的 18 个 commits 中是否有需要 cherry-pick 到 master 的修复（`efa8a52 fix: restore menus.py from escaped quotes`）
