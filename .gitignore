# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
*.egg
*.parquet
*.csv
_tmp*
_t.py

# Node
node_modules/
dist/
build/
*.log
npm-debug.log*
.yarn-cache

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.git*

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
*.local
.token*

# Database
*.db
*.sqlite
quarantine*

# Misc
*.log
*.err
.cache/

# Documents
docs/notes
docs/screen
docs/repos
docs/skills

# Test
# ⚠️ 长期回归测试**入库**（换机器/恢复环境后仍要有测试，见审计 D-7）。
# 这里只排除「跑测试时产生的临时物」，**不要**再用 `tests/` + `test_*` 一刀切
# —— 那会把 backend/tests、data-cleaner/tests 整个挡在版本库外。
__pycache__/
.pytest_cache/
# 测试临时工作区（内容形如 test_<case>0/1/…，是运行产物不是用例源码）
.pytest_tmp/

# AI temp
*.txt
_t_*.py

# 测试夹具放行 —— 必须放在 `*.txt` / `*.csv` 之后（gitignore 是「最后匹配胜出」），
# 否则夹具文件会被上面两条通用规则吞掉。
!**/tests/fixtures/**
!**/tests/**/fixtures/**

# Data Cleaner Raw Data
data-cleaner/data/ws/*.json
data-cleaner/data/intel/raw/*
data-cleaner/_upload_fenhong/*

# Credentials（含加密凭据与 token，禁止入库）
data-cleaner/user.json

# 一次性调试/探针脚本（保留 P1~P4 CLI 入口 _p[1-4]_*.py）
#
# 分类口径（D-13）：
#   - `_p[1-4]_*.py`  = 长期 CLI 入口，**入库**（上面几条规则刻意不覆盖它们）
#   - `_dbg_/_diag_/_test_/_q_/_poll_/_repair_/_rebuild_/_manual_` = 一次性探针，忽略
#   - `_verify_*` 大多是探针，但**极少数是长期验收工具** → 见下方例外
data-cleaner/_dbg_*.py
data-cleaner/_diag_*.py
data-cleaner/_test_*.py
data-cleaner/_verify_*.py
data-cleaner/_q_*.py
data-cleaner/_poll_*.py
data-cleaner/_repair_*.py
data-cleaner/_rebuild_*.py
data-cleaner/_manual_*.py
data-cleaner/_oa.json
backend/_manual_*.py
# 例外：长期保留的验收工具（可重复跑、有维护价值），要入库
# （`!` 例外必须放在上面的通用规则**之后**才生效 —— gitignore 是最后匹配胜出）
!data-cleaner/_verify_d4_task.py
# D-4 定时任务在运行进程生效的验收（上一条）
# D-8 画像超额收益人工核对工具（3 作者 × 10 提及独立复算 + 三点可复核样例）
!data-cleaner/_d8_verify.py
!data-cleaner/_d8_spotcheck.py

# 运行状态与报告产物
data-cleaner/data/*_state.json
data-cleaner/data/*.out
data-cleaner/data/missing_symbols.json

# data-cleaner/data 下的运行产物（日志/状态/parquet/中间数据集，勿入库）
data-cleaner/data/*.log
data-cleaner/data/*.err
data-cleaner/data/**/*.parquet
data-cleaner/data/**/*.csv
data-cleaner/data/**/*.jsonl
data-cleaner/data/**/*.state.json
data-cleaner/data/ws_probe/
data-cleaner/data/ws/*.json
data-cleaner/data/ws/*.jsonl
data-cleaner/p2_baseline_report.html
data-cleaner/p4_style_report.html
